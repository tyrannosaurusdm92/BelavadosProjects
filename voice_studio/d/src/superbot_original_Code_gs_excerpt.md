# Superbot Code.gs source reference

The full Superbot Code.gs was used as integration input. Voice Studio now contains converted compatibility adapter routes in `b/backend.gs`. The original large Code.gs is not duplicated here as runtime because `b/backend.gs` is the deployable merged backend reference.

## Route families converted

- chats.create / chats.list / chats.messages / chats.send
- learning.record / learning.search
- tasks.fromText / tasks.list
- file.indexText / files.search
- project.coverage
- voice.simulate / voice.job.create
- source.auditManifest

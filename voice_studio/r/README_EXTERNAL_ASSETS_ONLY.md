# Voice Studio Resources Are Hosted Externally

The actual files that belong under `voice_studio/r/` are intentionally omitted from this returned zip.

Use GitHub as the source of truth:

`https://tyrannosaurusdm92.github.io/BelavadosProjects/voice_studio/r/`

A full inventory of the omitted folder is stored here:

- `voice_studio/r/RESOURCE_FOLDER_MANIFEST.json`
- `voice_studio/manifests/folders/r_manifest.json`

Do not return these assets in edited Character Studio zips unless the user explicitly says: `send me voice studio assets`.

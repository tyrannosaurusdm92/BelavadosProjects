# TTS Placeholder

All TTS/runtime source files were intentionally removed from this stripped build.
The visible voice tools remain in `index.html`; attach replacement code through:

```js
window.BelavadosVoiceBridge.registerAdapter(yourVoiceAdapter)
```

# Media Folder

Use this folder for optional reference samples and visual assets. The scanner can record/import reference audio as influence metadata only. It does not clone voices.

`accent_reference_pack/` contains the small attached accent reference metadata/link pack, not a required runtime dependency.

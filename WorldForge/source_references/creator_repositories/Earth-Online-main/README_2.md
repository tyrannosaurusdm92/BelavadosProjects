# Earth_Online Local User Data

This directory is the default local data root for Earth_Online.

User photos, thumbnails, SQLite databases, vector indexes, backups, and exports are ignored by Git. A fresh clone starts with an empty local library; each user builds their own data by importing photos.

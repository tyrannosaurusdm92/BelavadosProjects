# Example settlement profiles

These folders mirror the requested human-readable example layout. The machine-readable versions used by the app are in `../settlement_biome_profiles/` and `../settlement_biome_profiles/index.json`.
